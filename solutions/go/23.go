package main

import (
  "fmt"
)

type node struct{
  val int
  next *node
  prev *node
}

var nodes map[int]*node
var head *node

func play(seq []int) {}

func main() {
  var input int
  fmt.Scanf("%d", &input)
  seq := make([]int, 0)
  for mask := 1; mask <= input; mask++ {
    seq = append(seq, )

  }

}
